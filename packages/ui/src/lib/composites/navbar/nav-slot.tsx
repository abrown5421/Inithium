import React from 'react';
import { Box } from '../../components';

const NavSlot: React.FC = () => {
  return (
    <Box flex align="center" padding="sm" className='h-[56px]'>
        nav
    </Box>
  )
};

export default NavSlot
